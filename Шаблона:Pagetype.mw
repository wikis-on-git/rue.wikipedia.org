{{#switch:{{NAMESPACE:{{#if:{{{page|}}}
  |{{{page}}}
  |{{#ifeq:{{{subjectspace|}}}|yes
   |{{TALKPAGENAME}}
   |{{FULLPAGENAME}}
  }}
 }}
}}
 |{{ns:Talk}}          = {{#switch:{{lc:{{{1|}}}}}
  |disambiguation|disambig|disamb|dab
  |redirect|redir
  |na                  = сторінка
  |#default            = статя
 }}
 |{{ns:User talk}}
 |{{ns:MediaWiki talk}}
 |{{ns:Help talk}}
 |{{ns:Portal talk}}
 |{{ns:Project talk}}  = сторінка
 |{{ns:Book talk}}     = книжка
 |{{ns:File talk}}     = файл
 |{{ns:Template talk}} = шаблона
 |{{ns:Category talk}} = катеґорія
 |#default             = {{#switch:{{lc:{{{1|}}}}}
  |portal
  |project
  |disambig|disamb|dab
  |redirect|redir
  |na                  = сторінка
  |book                = книжка
  |image|file          = файл
  |template|temp|templ = шаблона
  |category|cat|categ  = катеґорія
  |#default            = статя
 }}
}}<noinclude>
{{documentation}}
</noinclude>
